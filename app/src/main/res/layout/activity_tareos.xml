<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout_tareos"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="0dp"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_tareos"
        layout="@layout/app_bar_tareos"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view_tareos"
        android:verticalScrollbarPosition="left"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"

        android:background="@color/blancoOficial"
        app:headerLayout="@layout/nav_header_tareos"
        app:itemIconTint="@color/nav_item_text_selected"
        app:itemTextAppearance="@style/MenuItemTextAppearance"
        app:itemTextColor="@color/nav_item_text_selected"
        app:menu="@menu/activity_tareos_drawer" />
<!--    app:itemBackground="@color/nav_item_selected"-->

</androidx.drawerlayout.widget.DrawerLayout>